name: Test React App
on: push
jobs:
  test_app:
    runs-on: ubuntu-latest
    steps:
      - name: Get the repository code
        # run: echo
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Test app
        run: npm run test